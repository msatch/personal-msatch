---
phase: 03-home-page
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/home/problem-section.tsx
  - src/components/home/services-preview.tsx
  - src/components/home/process-section.tsx
  - src/app/[locale]/page.tsx
autonomous: true
requirements:
  - HOME-03
  - HOME-04
  - HOME-05

must_haves:
  truths:
    - "Problem/solution section presents LatAm pain points and M. Gripe's approach in both locales"
    - "Services preview shows 4 cards matching SERV-01 service lines, each linking to /services"
    - "How it works section displays 3 numbered steps describing the engagement process"
    - "All 5 sections render in correct conversion order: Hero, Problem, Services, Process, CTA Band"
    - "All visible text comes from translation files with zero hardcoded strings"
  artifacts:
    - path: "src/components/home/problem-section.tsx"
      provides: "Problem/solution section with pain points and approach"
      exports: ["ProblemSection"]
    - path: "src/components/home/services-preview.tsx"
      provides: "4 service preview cards linking to /services"
      exports: ["ServicesPreview"]
    - path: "src/components/home/process-section.tsx"
      provides: "How it works numbered steps"
      exports: ["ProcessSection"]
  key_links:
    - from: "src/components/home/services-preview.tsx"
      to: "/services"
      via: "next-intl Link component on each card"
      pattern: "Link.*href.*services"
    - from: "src/components/home/problem-section.tsx"
      to: "messages/es.json"
      via: "getTranslations('home.problem')"
      pattern: "getTranslations.*home\\.problem"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/home/*"
      via: "imports all 5 section components"
      pattern: "import.*components/home"
---

<objective>
Build the problem/solution, services preview, and process sections, then compose the complete home page with all 5 sections.

Purpose: Complete the home page conversion flow by implementing the three middle sections that build credibility and show the path forward, then assemble all 5 sections into the final page composition.

Output: problem-section.tsx, services-preview.tsx, process-section.tsx, and updated page.tsx with all 5 sections in conversion order.
</objective>

<execution_context>
@C:/Users/matia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-home-page/03-RESEARCH.md
@.planning/phases/03-home-page/03-01-SUMMARY.md
@messages/es.json
@messages/en.json
@src/app/[locale]/page.tsx
@src/components/home/hero-section.tsx
@src/components/home/cta-band.tsx
@src/i18n/navigation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build problem/solution and services preview sections</name>
  <files>src/components/home/problem-section.tsx, src/components/home/services-preview.tsx</files>
  <action>
**problem-section.tsx:**
Create an async Server Component for the problem/solution section (HOME-03).

- Import `getTranslations` from `next-intl/server`
- Use `getTranslations('home.problem')` for all section content
- Layout: two-column on desktop, stacked on mobile
  - Outer: `<section className="px-4 py-16 md:py-20 lg:py-24">`
  - Inner: `<div className="mx-auto max-w-6xl">`
  - Section title: h2 with `text-2xl md:text-3xl lg:text-4xl font-bold` centered
  - Section subtitle: p with `mt-4 text-base md:text-lg text-muted text-center max-w-3xl mx-auto`
  - Content grid: `<div className="mt-12 grid grid-cols-1 lg:grid-cols-2 gap-12">`
  - Left column (pain points): render 4 pain points using indexed keys `['1','2','3','4'].map(id => ...)` with `t('painPoints.${id}.title')` and `t('painPoints.${id}.description')`
  - Each pain point: title in `font-semibold text-lg` and description in `mt-1 text-muted`
  - Add vertical spacing between pain points: `space-y-6`
  - Right column (solution): render `t('solution.title')` as h3 and `t('solution.description')` as p
  - Solution styling: h3 with `text-xl font-bold` and p with `mt-3 text-muted leading-relaxed`
- Export as named export: `export async function ProblemSection()`
- NO `'use client'` directive

**services-preview.tsx:**
Create an async Server Component for the services preview cards (HOME-04).

- Import `getTranslations` from `next-intl/server` and `Link` from `@/i18n/navigation`
- Use `getTranslations('home.services')` for section content
- Layout: responsive grid of 4 cards
  - Outer: `<section className="bg-muted/30 px-4 py-16 md:py-20 lg:py-24">` (subtle background to differentiate from adjacent sections)
  - Inner: `<div className="mx-auto max-w-6xl">`
  - Section title: h2 centered, same pattern as problem section
  - Section subtitle: p centered, same pattern
  - Card grid: `<div className="mt-12 grid grid-cols-1 md:grid-cols-2 gap-6">`
  - Each card: `Link` to `/services` wrapping the card content
    - Card styling: `block p-6 rounded-lg border border-border bg-background hover:border-accent hover:shadow-md transition-all`
    - Card title: h3 with `text-lg font-bold`
    - Card description: p with `mt-2 text-sm text-muted`
  - Iterate over `['1','2','3','4']` with `t('items.${id}.title')` and `t('items.${id}.description')`
  - Below the grid, add a "View all services" link: `Link` to `/services` with `mt-8 inline-block text-accent font-semibold hover:underline` using `t('viewAll')` text
- Export as named export: `export async function ServicesPreview()`
- NO `'use client'` directive

**Note on bg-muted/30:** If `bg-muted/30` does not work with the project's Tailwind config (oklch colors may not support opacity modifier), use `bg-muted` or a light gray like `bg-gray-50` instead. Check existing usage in codebase.
  </action>
  <verify>
Verify both files exist and export the expected components. Run `npx tsc --noEmit` to check for TypeScript errors. Verify both components use `getTranslations` from `next-intl/server` (not `useTranslations`). Verify services-preview.tsx uses `Link` from `@/i18n/navigation` for card links. Verify no `'use client'` directive in either file. Verify iteration uses `['1','2','3','4']` (not arrays from JSON).
  </verify>
  <done>problem-section.tsx renders a two-column layout with 4 pain points on the left and the solution approach on the right, stacking on mobile. services-preview.tsx renders a 2-column grid of 4 service preview cards, each linking to /services via next-intl Link. Both components use Server Components with getTranslations, zero client JS.</done>
</task>

<task type="auto">
  <name>Task 2: Build process section and compose final page with all 5 sections</name>
  <files>src/components/home/process-section.tsx, src/app/[locale]/page.tsx</files>
  <action>
**process-section.tsx:**
Create an async Server Component for the "How it works" section (HOME-05).

- Import `getTranslations` from `next-intl/server`
- Use `getTranslations('home.process')` for section content
- Layout: numbered steps with badges
  - Outer: `<section className="px-4 py-16 md:py-20 lg:py-24">`
  - Inner: `<div className="mx-auto max-w-4xl">`
  - Section title: h2 centered, same pattern as other sections
  - Section subtitle: p centered, same pattern
  - Steps container: `<div className="mt-12 space-y-8">`
  - Each step: `<div className="flex gap-4 items-start">`
    - Number badge: `<div className="flex-shrink-0 w-10 h-10 rounded-full bg-accent text-white flex items-center justify-center font-bold text-lg">{id}</div>`
    - Content: `<div>` with title as h3 `font-bold text-lg` and description as p `mt-1 text-muted`
  - Iterate over `['1','2','3']` with `t('steps.${id}.title')` and `t('steps.${id}.description')`
- Export as named export: `export async function ProcessSection()`
- NO `'use client'` directive

**page.tsx final composition:**
Update `src/app/[locale]/page.tsx` to compose all 5 sections in conversion order.

- Import all 5 components:
  - `HeroSection` from `@/components/home/hero-section` (already imported from Plan 01)
  - `ProblemSection` from `@/components/home/problem-section`
  - `ServicesPreview` from `@/components/home/services-preview`
  - `ProcessSection` from `@/components/home/process-section`
  - `CtaBand` from `@/components/home/cta-band` (already imported from Plan 01)
- Keep `setRequestLocale(locale)` and `generateStaticParams` from existing page
- Remove any remaining direct `getTranslations` calls from page.tsx (translations are in section components)
- Render in conversion order:
  ```tsx
  <>
    <HeroSection />
    <ProblemSection />
    <ServicesPreview />
    <ProcessSection />
    <CtaBand />
  </>
  ```
- The page.tsx should be clean and minimal -- just locale setup and component composition
  </action>
  <verify>
Run `npm run build` to verify the complete page compiles. Check that page.tsx imports all 5 section components. Check the render order is Hero -> Problem -> Services -> Process -> CtaBand. Verify process-section.tsx exists with correct export. Verify no `'use client'` in process-section.tsx. Run `npm run dev` and check /es/ and /en/ render all 5 sections with translated content.
  </verify>
  <done>process-section.tsx renders 3 numbered steps with badges describing the engagement process. page.tsx composes all 5 sections in conversion order: Hero, Problem/Solution, Services Preview, How It Works, CTA Band. The complete home page renders in both /es/ and /en/ with fully translated content. Build passes cleanly. The page contains zero hardcoded strings and zero client-side JavaScript from home page components.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no TypeScript or compilation errors
2. All 5 section components exist under `src/components/home/` as async Server Components
3. page.tsx composes all 5 sections in conversion order: Hero, Problem, Services, Process, CTA Band
4. Visiting /es/ shows all 5 sections with Spanish content
5. Visiting /en/ shows all 5 sections with English content
6. Service preview cards link to /services via next-intl Link
7. All text on the page comes from translation files (zero hardcoded strings)
8. No `'use client'` directive in any home page section component
9. CTA buttons link to /contact (not buttons with onClick)
</verification>

<success_criteria>
- Problem/solution section shows 4 pain points and M. Gripe's approach in a two-column layout
- Services preview displays 4 cards matching SERV-01 service lines, each linking to /services
- Process section shows 3 numbered steps with badges describing the engagement flow
- All 5 sections render in conversion order on the complete home page
- Both /es/ and /en/ routes show fully translated content
- Zero hardcoded strings, zero client-side JS from section components
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-page/03-02-SUMMARY.md`
</output>
