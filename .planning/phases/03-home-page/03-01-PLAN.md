---
phase: 03-home-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/es.json
  - messages/en.json
  - src/components/home/hero-section.tsx
  - src/components/home/cta-band.tsx
  - src/app/[locale]/page.tsx
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - COPY-03
  - HOME-01
  - HOME-02
  - HOME-06

must_haves:
  truths:
    - "Hero section displays a compelling value proposition headline and subheadline in the current locale"
    - "Primary CTA button in hero reads 'Agenda tu diagnostico de 45 min' (ES) or 'Book your 45-min diagnostic' (EN) and links to /contact"
    - "CTA band at page bottom repeats the primary CTA on an accent-colored background"
    - "All visible text comes from translation JSON files with zero hardcoded strings"
    - "Spanish copy uses neutral-warm Mexican Spanish with conversational connectors and tu form"
    - "English copy conveys the same meaning in natural English, not a literal translation"
  artifacts:
    - path: "messages/es.json"
      provides: "Complete Spanish copy for all 5 home page sections"
      contains: "home.problem"
    - path: "messages/en.json"
      provides: "Complete English copy for all 5 home page sections"
      contains: "home.problem"
    - path: "src/components/home/hero-section.tsx"
      provides: "Hero section with headline, subtitle, and CTA link"
      exports: ["HeroSection"]
    - path: "src/components/home/cta-band.tsx"
      provides: "Full-width CTA band with accent background"
      exports: ["CtaBand"]
  key_links:
    - from: "src/components/home/hero-section.tsx"
      to: "/contact"
      via: "next-intl Link component"
      pattern: "Link.*href.*contact"
    - from: "src/components/home/cta-band.tsx"
      to: "/contact"
      via: "next-intl Link component"
      pattern: "Link.*href.*contact"
    - from: "src/components/home/hero-section.tsx"
      to: "messages/es.json"
      via: "getTranslations('home.hero')"
      pattern: "getTranslations.*home\\.hero"
---

<objective>
Generate all bilingual home page copy and build the hero section and CTA band components.

Purpose: Establish the complete content foundation for the home page (all 5 section namespaces in both languages) and implement the two bookend sections (hero at top, CTA band at bottom) that define the page's conversion flow.

Output: Expanded translation JSON files with full home page copy in both languages, hero-section.tsx, cta-band.tsx, and updated page.tsx composing these sections.
</objective>

<execution_context>
@C:/Users/matia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-home-page/03-RESEARCH.md
@messages/es.json
@messages/en.json
@src/app/[locale]/page.tsx
@src/i18n/navigation.ts
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate bilingual copy and expand translation JSON files</name>
  <files>messages/es.json, messages/en.json</files>
  <action>
Expand both translation JSON files with complete copy for all 5 home page sections. Write Spanish copy FIRST (primary market), then write natural English copy that conveys the same meaning.

**Spanish copy guidelines (COPY-01):**
- Neutral-warm Mexican Spanish base, no region-specific slang
- Use "tu" form (not "usted") for warmth and approachability
- Conversational connectors: "es decir", "en otras palabras", "por ejemplo"
- Short sentences, active voice, outcome-focused language
- Brand voice (COPY-03): clear, direct, strategic, senior but approachable, results-oriented

**English copy guidelines (COPY-02):**
- Natural English phrasing, NOT literal translation of Spanish
- Same tone: professional but warm, results-oriented
- Parallel meaning and structure to Spanish version

**Translation namespace structure to add under `home`:**

```json
{
  "home": {
    "hero": {
      "title": "...",
      "subtitle": "..."
    },
    "problem": {
      "title": "...",
      "subtitle": "...",
      "painPoints": {
        "1": { "title": "...", "description": "..." },
        "2": { "title": "...", "description": "..." },
        "3": { "title": "...", "description": "..." },
        "4": { "title": "...", "description": "..." }
      },
      "solution": {
        "title": "...",
        "description": "..."
      }
    },
    "services": {
      "title": "...",
      "subtitle": "...",
      "viewAll": "...",
      "items": {
        "1": { "title": "Asesoria Tecnica Estrategica", "description": "..." },
        "2": { "title": "Aceleracion de Delivery", "description": "..." },
        "3": { "title": "Alineacion de Producto y Negocio", "description": "..." },
        "4": { "title": "Liderazgo Fraccional", "description": "..." }
      }
    },
    "process": {
      "title": "...",
      "subtitle": "...",
      "steps": {
        "1": { "title": "...", "description": "..." },
        "2": { "title": "...", "description": "..." },
        "3": { "title": "...", "description": "..." }
      }
    },
    "ctaBand": {
      "title": "...",
      "subtitle": "..."
    }
  }
}
```

**Content source for copy:** Use PROJECT.md context:
- Positioning: "I help companies in LatAm convert business goals into technical execution with clarity, speed, and measurable outcomes."
- ICP: Founders/CEOs, technical/business managers, traditional business owners in digital transition
- Core pains: Slow delivery, misaligned teams, weak planning, lack of senior tech guidance
- Service lines: Strategic Technical Advisory, Delivery Acceleration, Product & Business Alignment, Fractional Leadership Support
- Entry offer: 45-minute diagnostic call + short action brief
- Brand voice: Clear, direct, strategic, senior but approachable, results-oriented

**Hero copy:** Keep the existing hero.title and hero.subtitle if they are strong, or improve them. The existing title "Convierto objetivos de negocio en ejecucion tecnica" is solid. Refine if needed.

**Pain points (4):** Slow delivery / missed deadlines, misaligned teams / tech-business gap, weak planning / reactive execution, lack of senior technical guidance.

**Service titles MUST match SERV-01:** Strategic Technical Advisory, Delivery Acceleration, Product & Business Alignment, Fractional Leadership Support (in both languages).

**Process steps (3):** Model the engagement flow: diagnostic call -> action plan -> execution support.

**CTA band:** A closing headline that creates urgency or reinforces value, with a subtitle.

Preserve all existing keys in both JSON files (metadata, common, footer, notFound, pages). Only expand the `home` namespace.

**IMPORTANT:** next-intl does NOT support array values. Use indexed object keys ("1", "2", "3", "4") as shown above.
  </action>
  <verify>
Both JSON files are valid JSON (no syntax errors). Run: `node -e "JSON.parse(require('fs').readFileSync('messages/es.json','utf8')); JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); console.log('Valid JSON')"`. Both files have identical key structures under `home` (same nesting, same keys). Spanish uses "tu" form, not "usted". No hardcoded English in es.json or hardcoded Spanish in en.json.
  </verify>
  <done>Both es.json and en.json contain complete copy for home.hero, home.problem (with 4 painPoints and solution), home.services (with 4 items matching SERV-01 service lines), home.process (with 3 steps), and home.ctaBand. Copy follows brand voice guidelines. Spanish and English have parallel structure with natural phrasing in each language.</done>
</task>

<task type="auto">
  <name>Task 2: Build hero section, CTA band, and update page composition</name>
  <files>src/components/home/hero-section.tsx, src/components/home/cta-band.tsx, src/app/[locale]/page.tsx</files>
  <action>
**hero-section.tsx:**
Create an async Server Component that renders the hero section with headline, subtitle, and primary CTA button.

- Import `getTranslations` from `next-intl/server` and `Link` from `@/i18n/navigation`
- Use `getTranslations('home.hero')` for section content and `getTranslations('common')` for CTA text
- Layout: centered text with responsive typography
  - `<section className="px-4 py-16 md:py-24 lg:py-32">`
  - Inner: `<div className="mx-auto max-w-4xl text-center">`
  - h1: `text-3xl md:text-5xl lg:text-6xl font-bold tracking-tight`
  - Subtitle p: `mt-6 text-base md:text-lg lg:text-xl text-muted max-w-2xl mx-auto`
  - CTA: `Link` to `/contact` with `mt-8 inline-block px-6 py-3 bg-accent text-white font-semibold rounded-lg hover:bg-accent-dark transition-colors text-base md:text-lg`
- Export as named export: `export async function HeroSection()`
- NO `'use client'` directive -- this is a Server Component

**cta-band.tsx:**
Create an async Server Component for the full-width CTA band at the bottom of the page.

- Import `getTranslations` from `next-intl/server` and `Link` from `@/i18n/navigation`
- Use `getTranslations('home.ctaBand')` for section content and `getTranslations('common')` for CTA text
- Layout: full-width accent background with centered content
  - `<section className="bg-foreground px-4 py-16 md:py-20">` (use bg-foreground for guaranteed white-on-dark contrast, per research pitfall 7)
  - Inner: `<div className="mx-auto max-w-3xl text-center">`
  - h2: `text-2xl md:text-3xl lg:text-4xl font-bold text-white`
  - Subtitle p: `mt-4 text-base md:text-lg text-white/80`
  - CTA: `Link` to `/contact` with `mt-8 inline-block px-8 py-4 bg-accent text-white font-bold rounded-lg hover:bg-accent-dark transition-colors text-base md:text-lg`
- Export as named export: `export async function CtaBand()`

**page.tsx update:**
Replace the current inline hero content in `src/app/[locale]/page.tsx` with component composition.

- Remove the current inline hero section JSX and the direct `getTranslations` calls
- Import `HeroSection` from `@/components/home/hero-section` and `CtaBand` from `@/components/home/cta-band`
- Keep `setRequestLocale(locale)` and `generateStaticParams`
- Render: `<><HeroSection /><CtaBand /></>` (Problem, Services, Process sections added in Plan 02)
- The page should use a fragment wrapper since the layout already provides `<main>`
  </action>
  <verify>
Run `npm run build` (or `npx next build`) to verify the page compiles with no errors. Check that the build succeeds and no TypeScript errors appear. Verify the components exist at the expected paths. Verify page.tsx imports both components. Verify no `'use client'` directive in either new component file. Verify both components use `Link` from `@/i18n/navigation` (not raw `<a>` or `<button>`).
  </verify>
  <done>hero-section.tsx renders the hero with translated headline, subtitle, and CTA Link to /contact. cta-band.tsx renders a full-width band with dark background, translated headline/subtitle, and CTA Link to /contact. page.tsx composes HeroSection and CtaBand as async Server Components. The current <button> element for CTA is replaced with a proper Link component. Build passes with no errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no TypeScript or compilation errors
2. Both JSON files are valid and have matching key structures under `home`
3. Visiting /es/ shows Spanish hero content and Spanish CTA band
4. Visiting /en/ shows English hero content and English CTA band
5. CTA buttons in both sections link to /contact (not buttons with no href)
6. All text on the page comes from translation files (zero hardcoded strings)
7. No `'use client'` directive in hero-section.tsx or cta-band.tsx
</verification>

<success_criteria>
- Both translation files contain complete bilingual copy for all 5 home page sections
- Hero section displays value proposition headline, subtitle, and CTA linking to /contact
- CTA band displays closing headline, subtitle, and CTA linking to /contact with dark background
- Spanish copy uses neutral Mexican Spanish "tu" form; English copy is natural, not literal translation
- Page composition works via async Server Components with zero client-side JS added
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-page/03-01-SUMMARY.md`
</output>
