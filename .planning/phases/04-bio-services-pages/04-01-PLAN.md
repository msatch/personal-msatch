---
phase: 04-bio-services-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/es.json
  - messages/en.json
  - src/components/bio/narrative-section.tsx
  - src/components/bio/photo-credentials.tsx
  - src/components/bio/social-proof-section.tsx
  - src/app/[locale]/bio/page.tsx
autonomous: true
requirements: [BIO-01, BIO-02, BIO-03, BIO-04, COPY-04]

must_haves:
  truths:
    - "Bio page presents a professional narrative framed through client outcomes, not a CV"
    - "Bio page displays a photo placeholder with initials MG on accent background"
    - "Bio page shows credentials and experience relevant to LatAm market"
    - "Bio page has a social proof placeholder section ready for future testimonials"
    - "Bio page ends with a CTA referencing the 45-minute diagnostic entry offer"
    - "All Bio page copy renders in both Spanish and English from translation files"
  artifacts:
    - path: "src/components/bio/narrative-section.tsx"
      provides: "Professional narrative section"
    - path: "src/components/bio/photo-credentials.tsx"
      provides: "Photo placeholder with initials and credentials grid"
    - path: "src/components/bio/social-proof-section.tsx"
      provides: "Social proof placeholder section"
    - path: "src/app/[locale]/bio/page.tsx"
      provides: "Bio page composing all sections"
    - path: "messages/es.json"
      provides: "Spanish bio namespace copy"
      contains: "bio"
    - path: "messages/en.json"
      provides: "English bio namespace copy"
      contains: "bio"
  key_links:
    - from: "src/components/bio/narrative-section.tsx"
      to: "messages/es.json"
      via: "getTranslations('bio.narrative')"
      pattern: "getTranslations.*bio\\.narrative"
    - from: "src/components/bio/photo-credentials.tsx"
      to: "messages/es.json"
      via: "getTranslations('bio.credentials')"
      pattern: "getTranslations.*bio\\.credentials"
    - from: "src/app/[locale]/bio/page.tsx"
      to: "src/components/bio/*"
      via: "imports and composes all bio sections"
      pattern: "import.*components/bio"
---

<objective>
Build the complete Bio page with professional narrative, photo placeholder, credentials, social proof placeholder, and a CTA section -- all with bilingual copy.

Purpose: Give visitors a trust-building page that positions M. Gripe through client outcomes and LatAm expertise, ending with a clear path to contact.
Output: Fully rendered Bio page at /es/bio and /en/bio replacing the current stub.
</objective>

<execution_context>
@C:/Users/matia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bio-services-pages/04-RESEARCH.md
@.planning/phases/03-home-page/03-01-SUMMARY.md
@.planning/phases/03-home-page/03-02-SUMMARY.md
@src/components/home/hero-section.tsx
@src/components/home/cta-band.tsx
@src/app/[locale]/page.tsx
@messages/es.json
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate bilingual Bio page copy and add to translation JSON files</name>
  <files>messages/es.json, messages/en.json</files>
  <action>
Add a top-level `bio` namespace to both es.json and en.json (do NOT nest under `pages.bio`). Keep the existing `pages.bio` stub keys for now (no breakage). Structure:

```json
"bio": {
  "pageTitle": "...",
  "hero": {
    "title": "...",
    "subtitle": "..."
  },
  "narrative": {
    "title": "...",
    "paragraphs": {
      "1": "...",
      "2": "...",
      "3": "..."
    }
  },
  "credentials": {
    "title": "...",
    "items": {
      "1": { "title": "...", "description": "..." },
      "2": { "title": "...", "description": "..." },
      "3": { "title": "...", "description": "..." },
      "4": { "title": "...", "description": "..." }
    }
  },
  "socialProof": {
    "title": "...",
    "subtitle": "...",
    "comingSoon": "..."
  },
  "cta": {
    "title": "...",
    "subtitle": "..."
  }
}
```

**Spanish copy guidelines (write first):**
- Neutral Mexican Spanish, tu form, conversational connectors
- Brand voice: clear, direct, strategic, senior but approachable, results-oriented
- Narrative (3 paragraphs): Frame EVERYTHING through client outcomes, NOT a resume. Paragraph 1: what M. Gripe does and for whom (outcome-framed). Paragraph 2: expertise and approach (why this works). Paragraph 3: LatAm connection and personal motivation. Do NOT list job titles or companies. Use "you/your" more than "I."
- Credentials (4 items): Each has a short title + description. Emphasize LatAm relevance, technical depth, business impact. Examples: "15+ years in tech leadership", "50+ companies across LatAm", "Bilingual ES/EN", "From startup to enterprise scale." These are outcome-signals, not resume lines.
- Social proof: Title like "What clients say" / "Lo que dicen nuestros clientes", subtitle explaining testimonials are coming soon, a comingSoon placeholder message.
- CTA: Reference the 45-minute diagnostic call per COPY-04. Softer tone than services CTA -- "Want to discuss how I can help your team?" / "Quieres conversar sobre como puedo ayudar a tu equipo?"
- Hero: A page-level heading and brief subtitle positioning the bio section.

**English copy:** Write independently in natural English (not a translation). Same structure, same tone, different phrasing.

Validate both files parse as valid JSON after editing.
  </action>
  <verify>Run `node -e "JSON.parse(require('fs').readFileSync('messages/es.json','utf8')); JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); console.log('Valid JSON')"` -- must output "Valid JSON". Confirm `bio` namespace exists at top level in both files with all sub-keys (narrative.paragraphs.1/2/3, credentials.items.1/2/3/4, socialProof.title/subtitle/comingSoon, cta.title/subtitle).</verify>
  <done>Both es.json and en.json contain complete `bio` namespace with professional narrative (3 paragraphs), 4 credentials, social proof placeholder, and CTA copy -- all framed through client outcomes, not CV-style.</done>
</task>

<task type="auto">
  <name>Task 2: Build Bio page section components and compose page</name>
  <files>src/components/bio/photo-credentials.tsx, src/components/bio/narrative-section.tsx, src/components/bio/social-proof-section.tsx, src/app/[locale]/bio/page.tsx</files>
  <action>
Create 3 async Server Components in `src/components/bio/` and update the bio page:

**1. `photo-credentials.tsx` (BIO-02, BIO-03):**
- Async Server Component using `getTranslations('bio')` for hero heading + credentials.
- Top part: Page hero with title and subtitle from `bio.hero.*`.
- Below hero: Side-by-side layout on desktop (flex row), stacked on mobile.
- Left side: Circular photo placeholder `<div>` -- 160x160 on mobile (`w-40 h-40`), 192x192 on md+ (`md:w-48 md:h-48`), `rounded-full bg-accent/10`, centered initials "MG" in `text-4xl md:text-5xl font-bold text-accent`.
- Right side: Credentials grid. Use `getTranslations('bio.credentials')`. Heading from `t('title')`. Map over `['1','2','3','4']` rendering each credential as a compact block: `t(\`items.${id}.title\`)` in `font-semibold` and `t(\`items.${id}.description\`)` in `text-muted text-sm`. Layout: 2-column grid on md+ (`md:grid-cols-2 gap-4`), stacking on mobile.
- Section spacing: Follow established convention `px-4 py-16 md:py-20 lg:py-24`, `mx-auto max-w-4xl`.

**2. `narrative-section.tsx` (BIO-01):**
- Async Server Component using `getTranslations('bio.narrative')`.
- Heading from `t('title')` using `text-2xl md:text-3xl font-bold`.
- Map over `['1','2','3']` rendering `t(\`paragraphs.${id}\`)` as `<p>` elements with `text-base md:text-lg text-muted leading-relaxed` and `mt-4` spacing.
- Section spacing: `px-4 py-16 md:py-20`, `mx-auto max-w-4xl`.
- Alternating background: `bg-muted/30` for visual differentiation.

**3. `social-proof-section.tsx` (BIO-04):**
- Async Server Component using `getTranslations('bio.socialProof')`.
- Section with centered text: `t('title')` as h2, `t('subtitle')` as subtitle, `t('comingSoon')` in italic muted text.
- Section spacing: `px-4 py-16 md:py-20`, `mx-auto max-w-4xl text-center`.

**4. Update `bio/page.tsx`:**
- Replace the existing stub content entirely.
- Import and compose: `PhotoCredentials`, `NarrativeSection`, `SocialProofSection`.
- Add a CTA section at the bottom: import `getTranslations` and `Link` from `@/i18n/navigation`. Create an inline CTA section (same visual pattern as home CTA band: `bg-foreground`, white text, accent button linking to `/contact` with `common.cta` button text). Use `bio.cta` namespace for title/subtitle.
- Keep `generateStaticParams` and `setRequestLocale` pattern.
- Remove the old `getTranslations('pages.bio')` import and usage.
- Fragment wrapper (`<>...</>`) composing all sections.

Follow exact import patterns from Phase 3: `import { getTranslations } from 'next-intl/server'`, `import { Link } from '@/i18n/navigation'`.
  </action>
  <verify>Run `npm run build` -- must pass with no errors. Verify all 3 component files exist in `src/components/bio/`. Verify `src/app/[locale]/bio/page.tsx` no longer contains "pages.bio" references and imports from `@/components/bio/`.</verify>
  <done>Bio page at /es/bio and /en/bio renders: hero heading, photo placeholder with MG initials, 4 credentials in a grid, professional narrative (3 paragraphs), social proof placeholder, and CTA band with entry offer link to /contact. Zero hardcoded strings. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. Both /es/bio and /en/bio routes exist in the build output
3. Bio page renders photo placeholder with "MG" initials
4. Bio page narrative is outcome-framed (no job titles or company names in copy)
5. Bio page credentials show 4 items relevant to LatAm market
6. Bio page social proof section has placeholder for future testimonials
7. Bio page CTA links to /contact with entry offer messaging
8. All visible text comes from translation JSON files (zero hardcoded strings)
</verification>

<success_criteria>
- Bio page fully replaces the stub at /es/bio and /en/bio
- All 4 BIO requirements (BIO-01 through BIO-04) are implemented
- COPY-04 entry offer messaging present in Bio page CTA
- Build passes cleanly for both locales
</success_criteria>

<output>
After completion, create `.planning/phases/04-bio-services-pages/04-01-SUMMARY.md`
</output>
