---
phase: 04-bio-services-pages
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - messages/es.json
  - messages/en.json
  - src/components/services/offerings-section.tsx
  - src/components/services/process-section.tsx
  - src/components/services/faq-section.tsx
  - src/components/services/services-cta.tsx
  - src/app/[locale]/services/page.tsx
autonomous: true
requirements: [SERV-01, SERV-02, SERV-03, SERV-04, SERV-05, COPY-04]

must_haves:
  truths:
    - "Services page displays 4 outcome-based service cards each with problem, approach, and outcome"
    - "Services page has a detailed 5-step How it works process section"
    - "Services page has a static FAQ section with 6 Q&A pairs covering scope, timeline, pricing, communication"
    - "Services page ends with a CTA linking to /contact with 45-minute diagnostic entry offer"
    - "Service card titles match the 4 service lines: Strategic Technical Advisory, Delivery Acceleration, Product & Business Alignment, Fractional Leadership Support"
    - "All Services page copy renders in both Spanish and English from translation files"
  artifacts:
    - path: "src/components/services/offerings-section.tsx"
      provides: "4 service cards with problem/approach/outcome"
    - path: "src/components/services/process-section.tsx"
      provides: "Detailed 5-step engagement process"
    - path: "src/components/services/faq-section.tsx"
      provides: "Static FAQ Q&A list"
    - path: "src/components/services/services-cta.tsx"
      provides: "Services page CTA band with entry offer"
    - path: "src/app/[locale]/services/page.tsx"
      provides: "Services page composing all sections"
    - path: "messages/es.json"
      provides: "Spanish services namespace copy"
      contains: "services"
    - path: "messages/en.json"
      provides: "English services namespace copy"
      contains: "services"
  key_links:
    - from: "src/components/services/offerings-section.tsx"
      to: "messages/es.json"
      via: "getTranslations('services')"
      pattern: "getTranslations.*services"
    - from: "src/components/services/faq-section.tsx"
      to: "messages/es.json"
      via: "getTranslations('services.faq')"
      pattern: "getTranslations.*services\\.faq"
    - from: "src/components/services/services-cta.tsx"
      to: "/contact"
      via: "Link href=/contact"
      pattern: "href.*contact"
    - from: "src/app/[locale]/services/page.tsx"
      to: "src/components/services/*"
      via: "imports and composes all services sections"
      pattern: "import.*components/services"
---

<objective>
Build the complete Services page with 4 outcome-based service cards, detailed process section, FAQ, and CTA -- all with bilingual copy.

Purpose: Give visitors a detailed view of M. Gripe's service offerings framed through client outcomes, with a clear path to the 45-minute diagnostic call.
Output: Fully rendered Services page at /es/services and /en/services replacing the current stub.
</objective>

<execution_context>
@C:/Users/matia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bio-services-pages/04-RESEARCH.md
@.planning/phases/04-bio-services-pages/04-01-SUMMARY.md
@src/components/home/cta-band.tsx
@src/components/home/process-section.tsx
@src/components/home/services-preview.tsx
@messages/es.json
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate bilingual Services page copy and add to translation JSON files</name>
  <files>messages/es.json, messages/en.json</files>
  <action>
Add a top-level `services` namespace to both es.json and en.json (do NOT nest under `pages.services`). Keep the existing `pages.services` stub keys for now. Structure:

```json
"services": {
  "pageTitle": "...",
  "hero": {
    "title": "...",
    "subtitle": "..."
  },
  "labels": {
    "problem": "...",
    "approach": "...",
    "outcome": "..."
  },
  "offerings": {
    "1": {
      "title": "Asesoria Tecnica Estrategica",
      "problem": "...",
      "approach": "...",
      "outcome": "..."
    },
    "2": {
      "title": "Aceleracion de Delivery",
      "problem": "...",
      "approach": "...",
      "outcome": "..."
    },
    "3": {
      "title": "Alineacion de Producto y Negocio",
      "problem": "...",
      "approach": "...",
      "outcome": "..."
    },
    "4": {
      "title": "Liderazgo Fraccional",
      "problem": "...",
      "approach": "...",
      "outcome": "..."
    }
  },
  "process": {
    "title": "...",
    "subtitle": "...",
    "steps": {
      "1": { "title": "...", "description": "..." },
      "2": { "title": "...", "description": "..." },
      "3": { "title": "...", "description": "..." },
      "4": { "title": "...", "description": "..." },
      "5": { "title": "...", "description": "..." }
    }
  },
  "faq": {
    "title": "...",
    "items": {
      "1": { "question": "...", "answer": "..." },
      "2": { "question": "...", "answer": "..." },
      "3": { "question": "...", "answer": "..." },
      "4": { "question": "...", "answer": "..." },
      "5": { "question": "...", "answer": "..." },
      "6": { "question": "...", "answer": "..." }
    }
  },
  "cta": {
    "title": "...",
    "subtitle": "..."
  }
}
```

**Spanish copy guidelines (write first):**
- Same brand voice as all existing copy: clear, direct, strategic, senior but approachable, results-oriented. Neutral Mexican Spanish, tu form.
- Service titles MUST match exactly: "Asesoria Tecnica Estrategica", "Aceleracion de Delivery", "Alineacion de Producto y Negocio", "Liderazgo Fraccional" (with proper accents).
- Each service card: problem = what the client is struggling with (2-3 sentences, client perspective), approach = how M. Gripe addresses it (2-3 sentences), outcome = what the client gets (2-3 sentences, specific and measurable where possible). DO NOT start with "We offer" or "Our service includes." Start problems with the client's pain.
- Labels: "El desafio" / "Enfoque" / "Resultado esperado" (not literal translations of "problem/approach/outcome").
- Process: 5 detailed steps (MORE detailed than home page 3-step version). Steps: (1) diagnostic call, (2) deep assessment/audit, (3) action plan delivery, (4) execution with support, (5) periodic review and adjustment. Each step has a title and 2-3 sentence description.
- FAQ: 6 Q&A pairs. Topics per SERV-04: scope of engagement, typical timeline, pricing model (value-based, not hourly), communication cadence (weekly syncs, async updates), what happens after the diagnostic call, industries/company types served. Each answer should be 2-3 sentences -- substantive but scannable.
- CTA: Direct entry offer messaging per COPY-04. "Start with a free 45-minute diagnostic" / "Empieza con un diagnostico gratuito de 45 minutos."
- Hero: Page heading + brief positioning subtitle.

**English copy:** Write independently in natural English (not a translation). Same structure, same messaging, different phrasing. Service titles in English: "Strategic Technical Advisory", "Delivery Acceleration", "Product & Business Alignment", "Fractional Leadership Support".

**IMPORTANT anti-patterns to AVOID:**
- Service cards that read like feature lists instead of outcome stories
- Duplicating the exact same content as home page service previews (these are expanded versions with problem/approach/outcome -- deeper and more detailed)
- Inconsistent entry offer messaging -- always use "45-minute diagnostic" / "diagnostico de 45 minutos"
- FAQ answers shorter than 2 sentences or longer than 5 sentences

Validate both files parse as valid JSON after editing.
  </action>
  <verify>Run `node -e "JSON.parse(require('fs').readFileSync('messages/es.json','utf8')); JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); console.log('Valid JSON')"` -- must output "Valid JSON". Confirm `services` namespace exists at top level in both files with offerings (4 items with problem/approach/outcome), process (5 steps), faq (6 items with question/answer), labels, hero, and cta sub-keys.</verify>
  <done>Both es.json and en.json contain complete `services` namespace with 4 outcome-based service offerings, 5-step process, 6 FAQ Q&A pairs, labels, hero, and CTA copy. All service card titles match the 4 established service lines. Entry offer messaging is consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Build Services page section components and compose page</name>
  <files>src/components/services/offerings-section.tsx, src/components/services/process-section.tsx, src/components/services/faq-section.tsx, src/components/services/services-cta.tsx, src/app/[locale]/services/page.tsx</files>
  <action>
Create 4 async Server Components in `src/components/services/` and update the services page:

**1. `offerings-section.tsx` (SERV-01, SERV-02):**
- Async Server Component using `getTranslations('services')`.
- Page hero section at top: `t('hero.title')` as h1 (`text-3xl md:text-4xl lg:text-5xl font-bold`), `t('hero.subtitle')` as subtitle (`mt-4 text-base md:text-lg text-muted`). Centered, `max-w-4xl`.
- Below hero: 4 service cards in a responsive grid (`grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8`).
- Each card: `p-6 md:p-8 rounded-lg border border-border bg-background`. Inside:
  - Card title: `t(\`offerings.${id}.title\`)` in `text-xl md:text-2xl font-bold`
  - Three sub-sections with labels: each has a `<h4>` label (`text-sm font-semibold uppercase tracking-wider text-muted`) using `t('labels.problem')`, `t('labels.approach')`, `t('labels.outcome')`, followed by the content paragraph.
  - The outcome label uses `text-accent` instead of `text-muted` to visually distinguish results.
  - Space between sub-sections: `mt-4 space-y-4`.
- Map over `['1','2','3','4']`.
- Section spacing: `px-4 py-16 md:py-20 lg:py-24`, `mx-auto max-w-6xl`.

**2. `process-section.tsx` (SERV-03):**
- Async Server Component using `getTranslations('services.process')`.
- Section heading: `t('title')` and `t('subtitle')`.
- 5 steps, each with a numbered accent badge (same visual pattern as home page `process-section.tsx`): `flex-shrink-0 w-10 h-10 rounded-full bg-accent text-white font-bold flex items-center justify-center`.
- Map over `['1','2','3','4','5']`, render title + description for each step.
- Layout: flex column with gap-8 between steps. Each step is a flex row (badge + content).
- Section spacing: `bg-muted/30 px-4 py-16 md:py-20 lg:py-24`, `mx-auto max-w-4xl`.

**3. `faq-section.tsx` (SERV-04):**
- Async Server Component using `getTranslations('services.faq')`.
- Section heading: `t('title')` as h2.
- Static Q&A list (NO accordion, no JavaScript, no 'use client'). Map over `['1','2','3','4','5','6']`.
- Each item: `border-b border-border pb-6 mb-6 last:border-0 last:mb-0`. Question in `text-lg font-bold`, answer in `mt-2 text-muted leading-relaxed`.
- Section spacing: `px-4 py-16 md:py-20`, `mx-auto max-w-4xl`.

**4. `services-cta.tsx` (SERV-05, COPY-04):**
- Async Server Component using `getTranslations('services.cta')` and `getTranslations('common')`.
- Same visual pattern as home page CTA band: `bg-foreground px-4 py-16 md:py-20`, `mx-auto max-w-3xl text-center`.
- Title in `text-2xl md:text-3xl lg:text-4xl font-bold text-white`.
- Subtitle in `mt-4 text-base md:text-lg text-white/80`.
- CTA button: `Link href="/contact"` with `mt-8 inline-block px-8 py-4 bg-accent text-white font-bold rounded-lg hover:bg-accent-dark transition-colors text-base md:text-lg`. Button text from `tCommon('cta')`.
- Import `Link` from `@/i18n/navigation`.

**5. Update `services/page.tsx`:**
- Replace the existing stub content entirely.
- Import and compose: `OfferingsSection`, `ProcessSection`, `FaqSection`, `ServicesCta`.
- Keep `generateStaticParams` and `setRequestLocale` pattern.
- Remove the old `getTranslations('pages.services')` import and usage.
- Fragment wrapper (`<>...</>`) composing all sections in order: Offerings (includes hero), Process, FAQ, CTA.

Follow exact import patterns from Phase 3: `import { getTranslations } from 'next-intl/server'`, `import { Link } from '@/i18n/navigation'`. Reference `src/components/home/process-section.tsx` for the numbered step badge pattern and `src/components/home/cta-band.tsx` for the CTA band pattern.
  </action>
  <verify>Run `npm run build` -- must pass with no errors. Verify all 4 component files exist in `src/components/services/`. Verify `src/app/[locale]/services/page.tsx` no longer contains "pages.services" references and imports from `@/components/services/`.</verify>
  <done>Services page at /es/services and /en/services renders: hero heading, 4 service cards with problem/approach/outcome, 5-step process with numbered badges, 6-item static FAQ, and CTA band with entry offer link to /contact. Zero hardcoded strings. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. Both /es/services and /en/services routes exist in the build output
3. Services page shows 4 service cards, each with problem/approach/outcome sub-sections
4. Service card titles match: Strategic Technical Advisory, Delivery Acceleration, Product & Business Alignment, Fractional Leadership Support (in current locale)
5. Process section shows 5 steps with numbered badges (more detailed than home page 3-step version)
6. FAQ section shows 6 Q&A pairs covering scope, timeline, pricing, communication, post-diagnostic, industries
7. CTA band links to /contact with entry offer messaging
8. All visible text comes from translation JSON files (zero hardcoded strings)
9. No 'use client' directive in any services component (all Server Components)
</verification>

<success_criteria>
- Services page fully replaces the stub at /es/services and /en/services
- All 5 SERV requirements (SERV-01 through SERV-05) are implemented
- COPY-04 entry offer messaging present in Services page CTA and service card outcomes
- Build passes cleanly for both locales
</success_criteria>

<output>
After completion, create `.planning/phases/04-bio-services-pages/04-02-SUMMARY.md`
</output>
